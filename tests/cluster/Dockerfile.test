# Dockerfile for running cluster tests inside Docker
# This allows running Redis Cluster integration tests on macOS/Windows
# where Docker internal IPs are not routable from the host.

FROM python:3.10-slim

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

# Install git (required for setuptools-scm)
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Default command runs the cluster tests
CMD ["uv", "run", "pytest", "tests/cluster/test_cluster.py", "-v", "-x"]
